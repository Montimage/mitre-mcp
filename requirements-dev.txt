# Development and Testing Dependencies
# Dev extras are declared in pyproject.toml ([project.optional-dependencies].dev)
# and pinned in uv.lock. Preferred install: uv sync --locked --extra dev
# This file delegates to the project metadata (PEP 621) as an editable install.
-e .[dev]
