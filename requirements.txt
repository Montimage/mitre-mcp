# Runtime dependencies are declared in pyproject.toml ([project].dependencies)
# and pinned in uv.lock. Preferred install: uv sync --locked --extra dev
# This file delegates to the project metadata (PEP 621).
.
