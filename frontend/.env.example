# MCP Server Configuration
VITE_MCP_DEFAULT_HOST=localhost
VITE_MCP_DEFAULT_PORT=8000

# LLM API keys are entered at runtime via the settings dialog
# (stored in browser IndexedDB) — never at build time.
